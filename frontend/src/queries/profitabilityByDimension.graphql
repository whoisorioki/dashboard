query ProfitabilityByDimension(
  $dimension: String!
  $startDate: String
  $endDate: String
) {
  profitabilityByDimension(
    dimension: $dimension
    startDate: $startDate
    endDate: $endDate
  ) {
    branch
    productLine
    itemGroup
    grossMargin
    grossProfit
  }
}
