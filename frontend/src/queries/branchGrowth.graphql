# Branch Growth Query
query BranchGrowth($startDate: String, $endDate: String) {
  branchGrowth(startDate: $startDate, endDate: $endDate) {
    branch
    monthYear
    monthlySales
    growthPct
  }
}
