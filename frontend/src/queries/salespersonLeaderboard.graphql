query SalespersonLeaderboard(
  $startDate: String
  $endDate: String
  $branch: String
  $productLine: String
) {
  salespersonLeaderboard(
    startDate: $startDate
    endDate: $endDate
    branch: $branch
    productLine: $productLine
  ) {
    salesperson
    salesAmount
    grossProfit
  }
}
