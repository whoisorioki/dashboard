query SalespersonLeaderboard(
  $startDate: String
  $endDate: String
  $branch: String
) {
  salespersonLeaderboard(
    startDate: $startDate
    endDate: $endDate
    branch: $branch
  ) {
    salesperson
    salesAmount
    grossProfit
  }
}
