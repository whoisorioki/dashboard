# Branch-Product Sales Heatmap Query
query branchProductHeatmap($startDate: String!, $endDate: String!, $branch: String, $productLine: String) {
  branchProductHeatmap(startDate: $startDate, endDate: $endDate, branch: $branch, productLine: $productLine) {
    branch
    product
    sales
  }
}
