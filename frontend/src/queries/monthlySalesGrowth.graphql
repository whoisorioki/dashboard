# Monthly Sales Growth Query
query monthlySalesGrowth($startDate: String!, $endDate: String!, $branch: String, $productLine: String) {
  monthlySalesGrowth(startDate: $startDate, endDate: $endDate, branch: $branch, productLine: $productLine) {
    date
    totalSales
    grossProfit
  }
}
