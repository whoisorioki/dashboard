import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import {
    Box,
    Button,
    Container,
    TextField,
    Typography,
    Alert,
} from '@mui/material';

// Authentication removed for testing
const Register = () => <div>Register page disabled for testing (authentication removed).</div>;
export default Register;
