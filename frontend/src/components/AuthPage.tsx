import React, { useState } from 'react'
import {
    Box,
    Paper,
    TextField,
    Button,
    Typography,
    Link,
    Alert,
    CircularProgress,
    Container,
    useTheme,
} from '@mui/material'
import { LoginOutlined, PersonAddOutlined } from '@mui/icons-material'
// Authentication removed for testing
const AuthPage = () => <div>Authentication page disabled for testing (authentication removed).</div>;
export default AuthPage;
