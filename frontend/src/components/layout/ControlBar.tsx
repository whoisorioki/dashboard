import React from "react";
import FilterBar from "../FilterBar";

const ControlBar: React.FC = () => {
  // TODO: Wire up props from context or parent as needed
  return <FilterBar />;
};

export default ControlBar;
